namespace MartainsOrder.Items.VanillaChanges
{
    /*public class KatanaMSwingStab : ModItem
	{
		bool swingStabState = false;
		
		public override string Texture => "Terraria/Item_" + ItemID.Katana;
		
		public override void SetStaticDefaults() {
			DisplayName.SetDefault("Katana");
			Tooltip.SetDefault("Switches between Swing and Stab");
		}

		public override void SetDefaults() {
			item.CloneDefaults(ItemID.Katana);
			item.useTime = item.useTime;
			item.useAnimation = item.useAnimation;
			if (swingStabState == false) {item.useStyle = 1; 
			
			}
			if (swingStabState == true) {item.useStyle = 3; 
			item.useTime = (item.useTime -1);
			item.useAnimation = (item.useAnimation -1);
			}
		}
		
		public override bool UseItem(Player player) {
			return true;
		if (swingStabState == false) {
			swingStabState = true;
			}
		if (swingStabState == true) {
			swingStabState = false;
			}
			return true;
		}

	}*/
}